
<%= form_for(@newpost) do |f| %>
    
  <div class="field">
    <b>New Tweet</b> <br/>
    <%= f.text_area :content, size: "30x3" %>
  </div>

  <div class="actions">
    <%= f.submit "Publish Tweet" %>
  </div>
    
<% end %>